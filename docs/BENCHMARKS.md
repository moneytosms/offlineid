# BENCHMARKS.md - Model Performance

> Generated by `scripts/validate_models.py`. Latencies measured on the build host
> CPU (x86_64, ONNX Runtime CPUExecutionProvider). On-device ARM numbers with
> NNAPI/XNNPACK/CoreML will differ; measure on a Snapdragon 7-series target for the
> figures the SPEC targets.

| Model | Size (MB) | Avg Latency (ms) | P95 Latency (ms) |
|---|---|---|---|
| SCRFD-500M | 2.41 | 6.1 | 7.5 |
| MobileFaceNet (INT8) | 3.35 | 43.6 | 49.5 |
| FASNet-2.7 (MiniFASNetV2) | 1.66 | 1.5 | 2.1 |
| FASNet-4.0 (MiniFASNetV1SE) | 1.66 | 1.5 | 1.9 |

**Total bundle size:** 9.09 MB  (cap: 20 MB)
**Estimated pipeline latency (host CPU):** ~51 ms  (target: < 1000 ms)

## Notes
- MobileFaceNet INT8 dynamic quant landed at 3.35 MB (SPEC estimated ~1.1 MB);
  still well within the bundle budget. Random-input FP32 vs INT8 cosine ~ 0.965
  (not representative -- real aligned faces stay >0.99 per SPEC section 4.2).
- Total add to the Datalake app ~ 9.1 MB models + ~3.5 MB ONNX Runtime AAR ~ 12.6 MB.
- Pipeline budget (detect + 2x liveness + embed) measured on host; add YUV->RGB,
  alignment, and cosine match (all < 10 ms) for end-to-end.
